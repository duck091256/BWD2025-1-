import React from 'react';

const Settings = () => {
  return (
    <div style={{ padding: 20 }}>
      <h2>⚙️ Settings</h2>
      <p>This is where user settings like password change, notification preferences, etc. will go.</p>
    </div>
  );
};

export default Settings;